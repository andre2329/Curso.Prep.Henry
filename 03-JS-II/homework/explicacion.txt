for = este es un bucle que nos sirve para hacer una operacion de manera repetitiva, ejemplo: tienes que pintar 100 cuadros entonces en codigo seria un bucle for 100 veces y dentro de este bucle esta la repeticion de la tarea a realizar.
`&&`, `||`, `!` = operadores, este tipo de operadores nos sirven para comparar 2 cosas el primero es AND, el resultado de esta operacion es verdadero solo si ambas partes son verdaderas, OR en esta caso devolvera verdadero si al menos una de las partes es verdadero y NOT negará el resultado es decir si es verdadero este operador lo convierte en falso y viceversa